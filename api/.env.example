# Required. Must match your public HTTPS tunnel URL when using GitHub Pages.
# For local-only use: http://localhost:9000/
API_URL=http://localhost:9000/

# Optional. Improves YouTube reliability on self-hosted instances.
# Start with: docker compose -f docker-compose.local.yml up -d
# YOUTUBE_SESSION_SERVER=http://localhost:8080/
